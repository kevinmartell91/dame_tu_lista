import { environment } from "../environments/environment";

export const APP_CONFIG = {
    apiBaseUrl: environment.apiBaseUrl,
    appBaseUrl: environment.appBaseUrl,
    sortQueryParams: 'sort',
    sortRequestParms : 'sort'
}